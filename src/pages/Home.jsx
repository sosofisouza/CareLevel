/**
 * pages/Home.jsx
 * PÁGINA INICIAL — rota: "/"
 * 
 * Exibe o banner de boas-vindas e os 6 cards de serviços.
 * Conecta-se com: Navbar (via Layout), nenhum serviço ainda.
 * 
 * Para adicionar dados reais: importe um hook, ex:
 *   import { useUser } from '../hooks/useUser'
 */
export default function Home() {
  return (
    <div>
      <h1>Eleve seu bem-estar todos os dias</h1>
      {/* TODO: Banner com carrossel */}
      {/* TODO: Grid de 6 cards de serviços */}
    </div>
  )
}
